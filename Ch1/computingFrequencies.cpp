#include <iostream>
#include <string>
#include <math.h>

std::string NumToPattern(int num, int k)
{
  std::string pattern;
  //convert num to sequence, keeping in mind length k
  return pattern;
}

int PatternToNum(std::string pattern) 
{
  //compute number based on sequence pattern
}

int computeFrequencies(std::string Text, int k) 
{
  int length = pow(4, k);
  int frequencyArray[length];
  for(int i = 0; i < length; ++i)
  {
    frequencyArray[i] = 0;
  }
}
